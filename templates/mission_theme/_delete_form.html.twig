<form method="post" action="{{ path('mission_theme_delete', {'id': mission_theme.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ mission_theme.id) }}">
    <button class="btn">Delete</button>
</form>
