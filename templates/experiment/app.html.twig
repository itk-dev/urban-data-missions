{% extends 'base.html.twig' %}

{% block title 'Experiment app'|trans %}

{% block content %}
    <div id="app"></div>
    {#
    <h1>{{ experiment.title }}</h1>

    <ul class="list-unstyled sensors">
        {% for sensor in experiment.sensors %}
            <li><a href="#{{ sensor.id }}">{{ sensor.id }}</a></li>
        {% endfor %}
    </ul>

    <fieldset>
        <legend>{{ 'Data'|trans }}</legend>

        <div id="chart"></div>

        <div id="log-entries"></div>
    </fieldset>
    #}
{% endblock %}

{% block stylesheets %}
    {{ encore_entry_link_tags('experiment/app') }}
{% endblock %}

{% block javascripts %}
    <script>
        var APP_OPTIONS = {{ app_options|default(null)|json_encode|raw }}
    </script>
    {{ encore_entry_script_tags('experiment/app') }}
{% endblock %}
