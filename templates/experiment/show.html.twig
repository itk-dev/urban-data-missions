{% extends 'base.html.twig' %}

{% block title %}Experiment{% endblock %}

{% block body %}
    <h1>Experiment</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ experiment.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ experiment.title }}</td>
            </tr>
            <tr>
                <th>Sensors</th>
                <td>{{ experiment.sensors ? experiment.sensors|json_encode : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('experiment_index') }}">back to list</a>

    <a href="{{ path('experiment_edit', {'id': experiment.id}) }}">edit</a>

    {{ include('experiment/_delete_form.html.twig') }}
{% endblock %}
